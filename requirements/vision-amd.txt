# AMD/Windows profile. DirectML provides the ONNX Runtime accelerator while
# Torch uses the portable CPU wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
-r base.txt

onnxruntime-directml==1.24.4; platform_system == "Windows"
onnxruntime==1.26.0; platform_system != "Windows"
torch==2.5.1+cpu
